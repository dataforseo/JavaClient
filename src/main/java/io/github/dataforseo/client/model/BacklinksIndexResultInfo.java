/*
 * DataForSEO API documentation
 * DataForSEO API is the starting point on your journey towards building powerful SEO software. With DataForSEO you can get all the data you need to build an efficient application while also saving your time and budget. DataForSEO API is using the REST technology for interchanging data between your application and our service. The data exchange is made through the widely used HTTP protocol, which allows applying our API to almost all programming languages.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package io.github.dataforseo.client.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.github.dataforseo.client.model.IndexHistory;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import org.openapitools.jackson.nullable.JsonNullable;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import io.github.dataforseo.client.JSON;

/**
 * BacklinksIndexResultInfo
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2024-09-04T13:45:49.667097300+03:00[Europe/Kiev]", comments = "Generator version: 7.8.0")
public class BacklinksIndexResultInfo {
  public static final String SERIALIZED_NAME_TOTAL_BACKLINKS = "total_backlinks";
  @SerializedName(SERIALIZED_NAME_TOTAL_BACKLINKS)
  private Long totalBacklinks;

  public static final String SERIALIZED_NAME_TOTAL_PAGES = "total_pages";
  @SerializedName(SERIALIZED_NAME_TOTAL_PAGES)
  private Long totalPages;

  public static final String SERIALIZED_NAME_TOTAL_DOMAINS = "total_domains";
  @SerializedName(SERIALIZED_NAME_TOTAL_DOMAINS)
  private Long totalDomains;

  public static final String SERIALIZED_NAME_INDEX_HISTORY = "index_history";
  @SerializedName(SERIALIZED_NAME_INDEX_HISTORY)
  private List<IndexHistory> indexHistory;

  public BacklinksIndexResultInfo() {
  }

  public BacklinksIndexResultInfo totalBacklinks(Long totalBacklinks) {
    this.totalBacklinks = totalBacklinks;
    return this;
  }

  /**
   * total number of backlinks our database contains for the moment of checking
   * @return totalBacklinks
   */
  @javax.annotation.Nullable
  public Long getTotalBacklinks() {
    return totalBacklinks;
  }

  public void setTotalBacklinks(Long totalBacklinks) {
    this.totalBacklinks = totalBacklinks;
  }


  public BacklinksIndexResultInfo totalPages(Long totalPages) {
    this.totalPages = totalPages;
    return this;
  }

  /**
   * total number of pages our database contains for the moment of checking
   * @return totalPages
   */
  @javax.annotation.Nullable
  public Long getTotalPages() {
    return totalPages;
  }

  public void setTotalPages(Long totalPages) {
    this.totalPages = totalPages;
  }


  public BacklinksIndexResultInfo totalDomains(Long totalDomains) {
    this.totalDomains = totalDomains;
    return this;
  }

  /**
   * total number of domains our database contains for the moment of checking
   * @return totalDomains
   */
  @javax.annotation.Nullable
  public Long getTotalDomains() {
    return totalDomains;
  }

  public void setTotalDomains(Long totalDomains) {
    this.totalDomains = totalDomains;
  }


  public BacklinksIndexResultInfo indexHistory(List<IndexHistory> indexHistory) {
    this.indexHistory = indexHistory;
    return this;
  }

  public BacklinksIndexResultInfo addIndexHistoryItem(IndexHistory indexHistoryItem) {
    if (this.indexHistory == null) {
      this.indexHistory = new ArrayList<>();
    }
    this.indexHistory.add(indexHistoryItem);
    return this;
  }

  /**
   * index volume data for the past 12 months
   * @return indexHistory
   */
  @javax.annotation.Nullable
  public List<IndexHistory> getIndexHistory() {
    return indexHistory;
  }

  public void setIndexHistory(List<IndexHistory> indexHistory) {
    this.indexHistory = indexHistory;
  }

  /**
   * A container for additional, undeclared properties.
   * This is a holder for any undeclared properties as specified with
   * the 'additionalProperties' keyword in the OAS document.
   */
  private Map<String, Object> additionalProperties;

  /**
   * Set the additional (undeclared) property with the specified name and value.
   * If the property does not already exist, create it otherwise replace it.
   *
   * @param key name of the property
   * @param value value of the property
   * @return the BacklinksIndexResultInfo instance itself
   */
  public BacklinksIndexResultInfo putAdditionalProperty(String key, Object value) {
    if (this.additionalProperties == null) {
        this.additionalProperties = new HashMap<String, Object>();
    }
    this.additionalProperties.put(key, value);
    return this;
  }

  /**
   * Return the additional (undeclared) property.
   *
   * @return a map of objects
   */
  public Map<String, Object> getAdditionalProperties() {
    return additionalProperties;
  }

  /**
   * Return the additional (undeclared) property with the specified name.
   *
   * @param key name of the property
   * @return an object
   */
  public Object getAdditionalProperty(String key) {
    if (this.additionalProperties == null) {
        return null;
    }
    return this.additionalProperties.get(key);
  }


  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    BacklinksIndexResultInfo backlinksIndexResultInfo = (BacklinksIndexResultInfo) o;
    return Objects.equals(this.totalBacklinks, backlinksIndexResultInfo.totalBacklinks) &&
        Objects.equals(this.totalPages, backlinksIndexResultInfo.totalPages) &&
        Objects.equals(this.totalDomains, backlinksIndexResultInfo.totalDomains) &&
        Objects.equals(this.indexHistory, backlinksIndexResultInfo.indexHistory)&&
        Objects.equals(this.additionalProperties, backlinksIndexResultInfo.additionalProperties);
  }

  private static <T> boolean equalsNullable(JsonNullable<T> a, JsonNullable<T> b) {
    return a == b || (a != null && b != null && a.isPresent() && b.isPresent() && Objects.deepEquals(a.get(), b.get()));
  }

  @Override
  public int hashCode() {
    return Objects.hash(totalBacklinks, totalPages, totalDomains, indexHistory, additionalProperties);
  }

  private static <T> int hashCodeNullable(JsonNullable<T> a) {
    if (a == null) {
      return 1;
    }
    return a.isPresent() ? Arrays.deepHashCode(new Object[]{a.get()}) : 31;
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class BacklinksIndexResultInfo {\n");
    sb.append("    totalBacklinks: ").append(toIndentedString(totalBacklinks)).append("\n");
    sb.append("    totalPages: ").append(toIndentedString(totalPages)).append("\n");
    sb.append("    totalDomains: ").append(toIndentedString(totalDomains)).append("\n");
    sb.append("    indexHistory: ").append(toIndentedString(indexHistory)).append("\n");
    sb.append("    additionalProperties: ").append(toIndentedString(additionalProperties)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("total_backlinks");
    openapiFields.add("total_pages");
    openapiFields.add("total_domains");
    openapiFields.add("index_history");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to BacklinksIndexResultInfo
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!BacklinksIndexResultInfo.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in BacklinksIndexResultInfo is not found in the empty JSON string", BacklinksIndexResultInfo.openapiRequiredFields.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      if (jsonObj.get("index_history") != null && !jsonObj.get("index_history").isJsonNull()) {
        JsonArray jsonArrayindexHistory = jsonObj.getAsJsonArray("index_history");
        if (jsonArrayindexHistory != null) {
          // ensure the json data is an array
          if (!jsonObj.get("index_history").isJsonArray()) {
            throw new IllegalArgumentException(String.format("Expected the field `index_history` to be an array in the JSON string but got `%s`", jsonObj.get("index_history").toString()));
          }

          // validate the optional field `index_history` (array)
          for (int i = 0; i < jsonArrayindexHistory.size(); i++) {
            IndexHistory.validateJsonElement(jsonArrayindexHistory.get(i));
          };
        }
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!BacklinksIndexResultInfo.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'BacklinksIndexResultInfo' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<BacklinksIndexResultInfo> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(BacklinksIndexResultInfo.class));

       return (TypeAdapter<T>) new TypeAdapter<BacklinksIndexResultInfo>() {
           @Override
           public void write(JsonWriter out, BacklinksIndexResultInfo value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             obj.remove("additionalProperties");
             // serialize additional properties
             if (value.getAdditionalProperties() != null) {
               for (Map.Entry<String, Object> entry : value.getAdditionalProperties().entrySet()) {
                 if (entry.getValue() instanceof String)
                   obj.addProperty(entry.getKey(), (String) entry.getValue());
                 else if (entry.getValue() instanceof Number)
                   obj.addProperty(entry.getKey(), (Number) entry.getValue());
                 else if (entry.getValue() instanceof Boolean)
                   obj.addProperty(entry.getKey(), (Boolean) entry.getValue());
                 else if (entry.getValue() instanceof Character)
                   obj.addProperty(entry.getKey(), (Character) entry.getValue());
                 else {
                   JsonElement jsonElement = gson.toJsonTree(entry.getValue());
                   if (jsonElement.isJsonArray()) {
                     obj.add(entry.getKey(), jsonElement.getAsJsonArray());
                   } else {
                     obj.add(entry.getKey(), jsonElement.getAsJsonObject());
                   }
                 }
               }
             }
             elementAdapter.write(out, obj);
           }

           @Override
           public BacklinksIndexResultInfo read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             JsonObject jsonObj = jsonElement.getAsJsonObject();
             // store additional fields in the deserialized instance
             BacklinksIndexResultInfo instance = thisAdapter.fromJsonTree(jsonObj);
             for (Map.Entry<String, JsonElement> entry : jsonObj.entrySet()) {
               if (!openapiFields.contains(entry.getKey())) {
                 if (entry.getValue().isJsonPrimitive()) { // primitive type
                   if (entry.getValue().getAsJsonPrimitive().isString())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsString());
                   else if (entry.getValue().getAsJsonPrimitive().isNumber())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsNumber());
                   else if (entry.getValue().getAsJsonPrimitive().isBoolean())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsBoolean());
                   else
                     throw new IllegalArgumentException(String.format("The field `%s` has unknown primitive type. Value: %s", entry.getKey(), entry.getValue().toString()));
                 } else if (entry.getValue().isJsonArray()) {
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), List.class));
                 } else { // JSON object
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), HashMap.class));
                 }
               }
             }
             return instance;
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of BacklinksIndexResultInfo given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of BacklinksIndexResultInfo
   * @throws IOException if the JSON string is invalid with respect to BacklinksIndexResultInfo
   */
  public static BacklinksIndexResultInfo fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, BacklinksIndexResultInfo.class);
  }

  /**
   * Convert an instance of BacklinksIndexResultInfo to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

